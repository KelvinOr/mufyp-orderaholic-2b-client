import { createTheme } from "@mui/system";

export default createTheme({
    palette: {
        primary: {
            main: "#607EAA",
        },
        secondary: {
            main: "#1C3879",
        },
    },
})